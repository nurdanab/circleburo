# 🗑️ Руководство по очистке медиа из Git

## Текущая ситуация

- ✅ Все медиа загружено в MinIO (404 MB)
- ✅ Код использует MinIO через `getMediaUrl()`
- ⚠️ Медиа еще в Git LFS (~404 MB в репозитории)

## Два варианта действий

### Вариант 1: Убрать медиа из Git (Рекомендуется) 🎯

**Что происходит:**
- Медиа удаляется из Git индекса
- Файлы остаются локально для разработки
- Production загружает с MinIO
- Репозиторий становится ~5 MB

**Шаги:**

1. **Запустить скрипт очистки:**
   ```bash
   ./scripts/remove-media-from-git.sh
   ```

2. **Проверить изменения:**
   ```bash
   git status
   ```
   Должно показать много удаленных файлов

3. **Закоммитить:**
   ```bash
   git add .
   git commit -m "🗑️ Remove media from git (now using MinIO cloud storage)"
   ```

4. **Отправить в репозиторий:**
   ```bash
   git push origin main
   ```

**Результат:**
- ✅ Репозиторий ~5 MB (было 404 MB)
- ✅ Медиа локально для разработки
- ✅ Production использует MinIO
- ✅ Быстрый git clone

---

### Вариант 2: Оставить как есть (Не рекомендуется)

**Что происходит:**
- Медиа остается в Git LFS
- Репозиторий остается большим
- Production все равно использует MinIO

**Когда использовать:**
- Если нужен бэкап медиа в Git
- Если команда не хочет зависеть от MinIO

**Минусы:**
- ❌ Большой размер репозитория
- ❌ Медленный git clone
- ❌ Дублирование (Git LFS + MinIO)

---

## Рекомендуемая стратегия

### Для Production деплоя (СЕЙЧАС):

```bash
# 1. Удалить медиа из Git
./scripts/remove-media-from-git.sh

# 2. Закоммитить изменения
git add .
git commit -m "🗑️ Remove media from git (now using MinIO)"
git push origin main

# 3. Задеплоить на Netlify
# (Netlify будет использовать MinIO через VITE_MEDIA_BASE_URL)
```

### Для локальной разработки:

```bash
# Медиа файлы уже есть локально в public/
# Они НЕ будут удалены, только из Git

# Если нужно скачать медиа заново:
npm run download-media  # (создадим скрипт если нужно)
```

---

## FAQ

### ❓ Что если удалю медиа из Git по ошибке?

**Ответ:** Медиа остается в MinIO и локально. Можно всегда:
1. Скачать из MinIO
2. Восстановить из Git истории (до коммита удаления)

### ❓ Как новый разработчик получит медиа?

**Ответ:** Два способа:
1. Скачать из MinIO (создадим скрипт)
2. Скопировать из бэкапа локально

### ❓ А если MinIO упадет?

**Ответ:**
1. У вас есть локальные копии
2. Можно быстро восстановить MinIO из бэкапа
3. Можно временно вернуть локальные пути (убрать VITE_MEDIA_BASE_URL)

### ❓ Влияет ли это на локальную разработку?

**Ответ:** НЕТ! Медиа остается в `public/` локально. Vite будет использовать:
- MinIO если есть `VITE_MEDIA_BASE_URL` в .env
- Локальные файлы если переменной нет

---

## Проверка после очистки

### 1. Проверить размер репозитория:
```bash
du -sh .git
# Должно быть ~5-10 MB (было ~400+ MB)
```

### 2. Проверить что медиа локально:
```bash
ls -lh public/img/
ls -lh public/videos/
# Файлы должны быть на месте
```

### 3. Проверить что Git игнорирует:
```bash
git status
# Не должно показывать файлы в public/img/, public/videos/ и т.д.
```

### 4. Проверить build:
```bash
npm run build
# Должно пройти успешно
```

---

## Что дальше?

После очистки Git:

1. **Деплой на Netlify** (см. DEPLOYMENT_CHECKLIST.md)
2. **Настроить скрипт скачивания медиа** (для новых разработчиков)
3. **Настроить бэкап MinIO** (опционально)
4. **Добавить CDN перед MinIO** (опционально)

---

## Быстрые команды

```bash
# Удалить медиа из Git
./scripts/remove-media-from-git.sh

# Проверить изменения
git status

# Закоммитить и отправить
git add .
git commit -m "🗑️ Remove media from git"
git push origin main

# Проверить размер
du -sh .git
```

**Готовы? Запустите скрипт!** 🚀
